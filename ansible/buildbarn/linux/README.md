# BuildBarn Deployment on linux

Buildbarn can either be deployed onto the same machine or separate machines. If the worker is installed, the runner will also automatically be installed on the same machine.
